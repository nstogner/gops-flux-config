apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: cluster-config
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://github.com/nstogner/gops-cluster-config
  ref:
    branch: main
